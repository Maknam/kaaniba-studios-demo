import StepShell from '../StepShell'

const contactMethods = ['WhatsApp', 'Phone', 'Email']

export default function ContactStep({ form, update }) {
  return <StepShell eyebrow="The person behind the project" title="Where should we send your design consultation?"><div className="contact-fields"><label className="wizard-field"><span>Full Name *</span><input autoComplete="name" required value={form.fullName} onChange={(event) => update({ fullName: event.target.value })} placeholder="Your full name" /></label><label className="wizard-field"><span>Email Address *</span><input autoComplete="email" type="email" required value={form.email} onChange={(event) => update({ email: event.target.value })} placeholder="you@example.com" /></label><label className="wizard-field"><span>Phone / WhatsApp *</span><input autoComplete="tel" type="tel" required value={form.phone} onChange={(event) => update({ phone: event.target.value })} placeholder="+233 ..." /></label><label className="wizard-field"><span>City / Location</span><input autoComplete="address-level2" value={form.location} onChange={(event) => update({ location: event.target.value })} placeholder="Accra, Ghana" /></label><label className="wizard-field full-field"><span>Briefly describe what you would like to change</span><textarea rows="4" value={form.description} onChange={(event) => update({ description: event.target.value })} placeholder="Tell us what is working, what is not, and how you want the space to feel." /></label></div><fieldset className="contact-method"><legend>Preferred contact method</legend>{contactMethods.map((method) => <label key={method}><input type="radio" name="contact-method" checked={form.preferredContactMethod === method} onChange={() => update({ preferredContactMethod: method })} />{method}</label>)}</fieldset><label className="consent"><input type="checkbox" checked={form.consent} onChange={(event) => update({ consent: event.target.checked })} /><span>I agree to be contacted regarding my design enquiry. *</span></label></StepShell>
}
